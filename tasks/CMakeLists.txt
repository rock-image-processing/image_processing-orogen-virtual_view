# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(virtual_viewTaskLib)
ADD_LIBRARY(${VIRTUAL_VIEW_TASKLIB_NAME} SHARED 
    ${VIRTUAL_VIEW_TASKLIB_SOURCES})
add_dependencies(${VIRTUAL_VIEW_TASKLIB_NAME}
    regen-typekit)

TARGET_LINK_LIBRARIES(${VIRTUAL_VIEW_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${VIRTUAL_VIEW_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${VIRTUAL_VIEW_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${VIRTUAL_VIEW_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${VIRTUAL_VIEW_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${VIRTUAL_VIEW_TASKLIB_HEADERS}
    DESTINATION include/orocos/virtual_view)

